Your job is to classify a high-level avionics test description into one of three categories:

Logical Expression. These requirements can be expressed purely in terms of variables and logical operations (AND, OR, NOT) and basic relational comparisons (>, <, ==), without any mathematical expressions. For example:

Output variable A shall be set to X when input variable B == 'valid' or input variable C is not empty.

Variable D shall be true if input variable E is 'active' and input variable F is 'enabled'.Note that requirements with literal string assignments, such as "Variable A shall be set to 'SUNNY'", also fall into this category.

String or data entry formats. This category is used to classify requirements that contain an input in a specific string format. These requirements will explicitly describe the arrangement of characters in an input field, using terms such as "format" and specifying the composition of the string components as alphanumeric digits, spaces, fixed characters, etc. Examples of requirements that fall into this category include:

Variable A shall be input in one of the following valid formats: "mmDDyyyy", "m/mDD", "mm/YYYY" where... (conditions on the various fields are laid out).

The system shall accept a string input in the format "ddd-A" where d is a digit between 1-9.

In contrast, requirements that simply specify a literal string output or assignment do not fall into this category. For example:

Variable A shall be set to "SUNNY" does not describe an input format and should be categorized as a logical expression.

Note that if a requirement combines a string format specification with a logical expression, it should be categorized as a "logical expression with string format". The key difference here is that the string format is for an input variable meaning that the output depends on this value.

Some requirements combine a string format specification together with a logical expression, but do not involve mathematical calculations. For example:

Variable A shall be output as XYZ when input variable B is 'valid' and input variable C is not empty, formatted as ddd/A where d is in 1-9.

The system shall accept a string input in the format "ddd-A" where d is a digit between 1-9, and set output variable X to 'true' if the input is valid.

These requirements are essentially a combination of logical operations (AND, OR, NOT) and formatting specifications, without any numerical calculations. Literal string assignments (e.g., A shall be output as Valid) do not fall into this category; these should simply be categorized as a "logical expression".

Mathematical Expressions: These requirements describe numerical relationships, calculations, or formulas involving variables. They may include arithmetic operations (+, -, \*, /), comparisons (<, >, =, <=, >=), or other mathematical functions (e.g., trigonometric, exponential). Examples include:

Variable X shall be calculated as the sum of Y and Z.

The value of A shall be determined by the formula A = 2 \* B + C.

Output variable D shall be set to the square root of input variable E when condition F is true.

The system shall calculate the average of variables G, H, and I.

If a requirement contains any mathematical operation, function, or relationship between variables, it falls into this category. This includes requirements that describe numerical thresholds, ratios, proportions, or other quantitative relationships.

Math Expression with String Format: Requirements that combine a mathematical expression with a string format specification for the output. To determine if a requirement falls into "logical expression with string format" or "math expression with string format" is to ask "does the value I'm setting depend on some other variables?". If the answer is yes, then it is an output and should be classified "math expression with string format". Another way to identify if the variable is an output is if the requirement specifies that this variable is "set" to a value or the variable appears "as" some format.

Respond in JSON format with a single key, "type", whose value must be either "logical expression", "string format", "logical expression with string format", "math expression", or "math expression with string format".

Here is the requirement description:

{req_description}
