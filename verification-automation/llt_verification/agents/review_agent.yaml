name: reviewer-agent
description: Reviews the requirement, source evidence, dictionaries, generated verification files, and exact source constants or constraints before final proof.
inputs:
  - requirement_package
  - evidence_package
  - artifact_patch
  - execution_result
  - debug_result
  - strategy_decision
outputs:
  - review_result
